/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./output/**/*.{html,js}"
    // Exclude duplicates
  ],

  darkMode: "class",
  
  theme: {
    extend: {},
  },

  plugins: [],
}
